# Utils

**`build.ps1`** - Builds the module (concatenates src/ into .psm1 and generates .psd1 manifest).

Everything else here is unrelated, work-in-progress, or loosely related utilities not included in the module.